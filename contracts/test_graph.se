inset('graph.se')

# Graph https://i.imgur.com/rfnk3qy.png
def populate_graph():
    # v1 -> ?
    set_edge_key("v1", "v2", "liability", 50)
    set_edge_key("v1", "vS", "liability", 100)
    set_edge_key("v1", "vB", "liability", 150)

    # v2 -> ?
    set_edge_key("v2", "v1", "liability", 75)
    set_edge_key("v2", "vS", "liability", 75)

    # v3 -> ?
    set_edge_key("v3", "vB", "liability", 150)
    set_edge_key("v3", "vS", "liability", 50)

    # vS -> ?
    set_edge_key("vS", "v3", "liability", 150)

    # vB -> ?
    set_edge_key("vB", "v1", "liability", 150)